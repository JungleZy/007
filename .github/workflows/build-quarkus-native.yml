name: Build Quarkus Native

on:
  push:
    branches: ['main', 'master']
  pull_request:

jobs:
  build:
    name: Build (${{ matrix.os }} / ${{ matrix.arch }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            arch: x86_64
            runner: ubuntu-22.04
          - os: ubuntu-24.04
            arch: arm64
            runner: ubuntu-24.04-arm
          - os: windows-2022
            arch: x86_64
            runner: windows-2022
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up GraalVM (Java 21 with Native Image)
        if: runner.os == 'Windows'
        uses: graalvm/setup-graalvm@v1
        with:
          distribution: graalvm
          java-version: '21'
          components: native-image

      - name: Cache Maven repository
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build Native Binary (Linux - GLIBC 2.17/2.23 Compatible)
        if: runner.os == 'Linux'
        run: |
          # Determine architecture for GraalVM download
          GRAAL_ARCH="linux-x64"
          if [ "${{ matrix.arch }}" = "arm64" ]; then
            GRAAL_ARCH="linux-aarch64"
          fi
          
          echo "Building for architecture: $GRAAL_ARCH"
          
          docker build --build-arg GRAALVM_ARCH=$GRAAL_ARCH -t quarkus-builder -f src/main/docker/Dockerfile.glibc-builder .
          docker run --rm -v ${{ github.workspace }}:/project -v ~/.m2:/root/.m2 quarkus-builder mvn -B -Pnative package -DskipTests

      - name: Build Native Binary (Windows)
        if: runner.os == 'Windows'
        run: mvn -B -Pnative package

      - name: Upload Native Artifact
        uses: actions/upload-artifact@v4
        with:
          name: quarkus-native-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            target/*-runner*
            target/*-runner.exe
          if-no-files-found: error

  release:
    name: Publish Release
    needs: [build]
    runs-on: ubuntu-22.04
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/**
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          draft: false
          prerelease: false
