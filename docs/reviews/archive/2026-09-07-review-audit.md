# 结论：汇总文档 93 项断言逐条复核——确认 84（90.3%）、部分成立 9、误报 0；1 处事实错误（PT-P1-02 误称「无鉴权」）与 8 处后果/影响面表述不精确已全部回改；计数与去重逻辑 10/10 成立，`P0 0 / P1 34 / P2 139 / P3 53 = 226` 可从分片原始 228 精确推出

| 项目 | 内容 |
|---|---|
| 审计对象 | `docs/reviews/2026-09-07-full-project-review.md`（本轮汇总评审文档），连带核对其引用的 11 份同日分片报告与 `/tmp/runtime-evidence-20260907.md` 运行证据 |
| 审计日期 | 2026-09-07 |
| 审计方式 | 5 个并行独立审计代理，按文档结构切片：①§3 前两表（事务与评分，17 条 + §2 引擎前提）②§3 后两表（功能与边界，18 条）③计数与去重逻辑（10 项）④§5 运行验证 / §6 核销 / §8 待验证（27 项）⑤§4 系统性根因 / §7 修复顺序 / 附录 / 页首元信息（21 项）。全部只读取证，未运行构建 |
| 判定口径 | 五分类：确认 / 部分成立（现象在但触发条件或后果有出入）/ 误报 / 已失效 / 待运行验证；逐条给当前 `file:line`，禁止抽样；行号漂移不算误报 |
| 审计结果 | 确认 84 / 部分成立 9 / 误报 0 / 已失效 0 / 待运行验证 0 |
| 处置 | 9 条「部分成立」全部已在汇总文档内回改并标注「审计修正」；1 条运行证据缺口已回填证据文件 |

---

## 1. 分片审计统计

| 审计分片 | 覆盖断言 | 确认 | 部分成立 | 误报 | 结论 |
|---|---|---|---|---|---|
| 事务与评分正确性（§3 前两表 + §2 引擎前提）| 18（表 17 行 + 1）| 14 | 3 | 0 | 17 条底层缺陷全部真实存在、位置准确、P1 定级恰当；3 条后果表述需收紧 |
| 功能与边界（§3 后两表）| 18 | 16 | 2 | 0 | 含唯一一处**事实错误**（PT-P1-02 的鉴权前提）|
| 计数与去重逻辑（页首/§1/§2/§3 行数/链接）| 10 | 10 | 0 | 0 | 三方对账全部一致，无计数错误，10 条链接全部命中 |
| 运行验证与跨轮核销（§5/§6/§8）| 27 | 25 | 2 | 0 | §5、§8 全部成立；§6 一处转引计数写错；一处证据未转录 |
| 系统性根因与修复顺序（§4/§7/附录/页首）| 21 | 19 | 2 | 0 | §4.2 命名规律过度概括；§7 步 4 可执行性低估影响面 |
| **合计** | **93** | **84** | **9** | **0** | |

无「误报」意味着：汇总文档指控的每一处缺陷都在当前源码中真实存在，问题只出在后果描述与影响面的精确度。

---

## 2. 已回改的 9 处（逐条）

| # | 位置 | 原表述问题 | 审计依据 | 回改后 |
|---|---|---|---|---|
| 1 | §3 `PT-P1-02` | **事实错误**：称 `GET /postTelegramTrain/test` 为「无鉴权」| `PostTelegramTrainController.java:42` 有类级 `@JWT`，端点需有效 token | 改为「任意已登录用户」，并明确本轮唯一无鉴权写路径只有 `GET /api/test/start` |
| 2 | §3 `SM-P1-03` | 称后果含「统计陈旧」| 触发条件本身是「无统计行」，不存在可陈旧的数据（`TelexPatService.java:100-101`）| 改为「删除已提交却返回 error，提交与返回码矛盾」|
| 3 | §3 `CI-P1-01` | 称错误比率「静默算成 0% 落库」| `statisticsScoreAndDotLineGapRate` 组装 `GeneralTickerPatTrainStatisticVO` 直接返回，未持久化 | 改为「报表展示为 0%，非落库」，并注明 13 处调用中仅 `:737` 出错 |
| 4 | §3 `TU-P1-02` | 称 `assignDefaultRole:255` 恒 null → 新用户登录 NPE | `UserService.java:256` 有 `if (defaultRole != null)` 守卫 | 改为「新建用户静默拿不到任何角色」，登录 NPE 标为未验证下游推断；确证核心是「编辑标量字段永不落库」|
| 5 | §3 `CA-P1-03` | 与 `CA-P1-01/02` 并列称「详情为空」| `SimulationRouterRoomService.java:257-258` 走 `orElseThrow(IllegalArgumentException)` | 单列其后果为显式错误信封（HTTP 200 + CODE_500）|
| 6 | §3 `CA-P1-04` | 称 `rows=0` 除零，未说明默认值 | `dto/Page.java:23`、`common/utils/Page.java:21` 的 `rows` 默认是 **20** | 补「默认 20，除零只在显式传 0 时发生」，并把 -1 越界定位到 `setFirstResult` |
| 7 | §3 `SM-P1-04` | 称「拆箱 NPE」| `RadiotelephoneService.java:56-57` 是对 null 实体直接解引用 | 改为「直接解引用 NPE（非拆箱）」，`parseInt` NFE 另列 |
| 8 | §4 第 2 条 | 「22 张 MyISAM **全部是** …四类命名，**恰好是** 评分结算目标表」| 实测 22 张中 `hand_key_err_log`（错误日志追加表）、`t_post_telegram_train_content_value`、`t_post_telegraph_key_pat_train_more` 三张不符该规律 | 去掉「全部/恰好」，改为枚举实际分类 + 明确「评分结算目标表确实在这 22 张之内」|
| 9 | §7 步 4 | 建议「删除 `ToolUtil.calculateRate` 三参版」| 该方法在 `GeneralTickerPatService` 有 13 处调用（:728-737、742、747、752），12 处依赖其「守首参」语义正常工作 | 改为「不可直接删除；最小修正是改对 `:737` 首参并统一守卫语义，再逐步迁往 `PatTrainStatisticsUtil`」|

另有 1 处非文档错误的处置：§6 转引持久层分片 §5 的计数原写「已修复 8 / 部分 3」，与分片实际（已修复 6 / 部分修复 4）不符，已改为「§5 共 29 行为已修复 6 / 部分修复 4 / 其余未修复（含 2 条判归其他分片、1 条汇总条目）」。

---

## 3. 定级复核

### 3.1 四条 P0 升级候选：全部维持 P1

审计对 `PT-P1-01`、`PT-P1-09`、`PT-P1-10`、`PS-P1-02` 独立给出定级结论，与汇总一致：

| 条目 | 维持 P1 的理由 |
|---|---|
| PT-P1-01（`TelegramTrainService.save` 吞异常）| 需畸形请求（缺 `trainFloors` 等）在写入中触发 NPE，非稳定路径；损坏限单用户自身训练态，旧暂停训练是被转为 `status=3` 而非被删；不满足「稳定造成永久丢失／跨用户错乱」|
| PT-P1-09 / PT-P1-10（delete→saveAndFlush 窗口）| 永久丢失只在两步之间发生中断/异常的窄窗口触发；正常执行两步皆成功、无损。属「非稳定造成」|
| PS-P1-02（22 张 MyISAM 不可回滚）| 系统性/部署层前提，自身不是离散可稳定触发的缺陷，须依赖上述结算操作失败才致损；是放大器而非独立 P0 |

### 3.2 `CA-P0-01` 由 P0 改级 P1：改级成立

审计独立结论与汇总一致，且给出了更完整的理由：损害完全取决于特定 UUID 是否存在于目标库（属环境/输入假设，非普适稳定触发）；即便命中也只覆写单行训练状态（status/validTime/mark/schedule），`t_ticker_tape_train` 为 InnoDB 事务化、无跨用户级联、可由重训恢复，不构成「永久数据损坏」。其「无鉴权写」的安全面按内网口径列为已接受风险，不回升 P0。

### 3.3 `SM-P1-05` 由 P1 降级 P2：降级成立

`t_cable`/`t_cable_floor`/`t_cable_type` 实测均为 InnoDB（活库 `information_schema` 与 `project006.sql:360/389/20607` 双向印证），NPE 逸出后事务回滚，无永久丢失。

---

## 4. 计数与去重审计（10/10 成立）

1. **三方对账**（分片页首声称 / 分片表格实际数据行 / 汇总 §1）：10 份分片全部一致，无一差额。聚合型条目（如 `CA-P2-04` 的「11 处转换」、`CA-P2-09` 的「20 个 GET 写」）各占一个编号、计一条，未虚增。
2. **原始合计逐列相加**：P0 1、P1 34、P2 140、P3 53，小计 228；按分片小计列相加同为 228。
3. **去重推导闭合**：P0→P1（`CA-P0-01`）跨级平移；P1→P2（`SM-P1-05`）跨级平移；两处合并各减 1 个 P2（`GP-P2-02`、`CA-P2-12`）；一处保留独立不变 → 终值 `0/34/139/53 = 226`，与页首结论行完全吻合。
4. **§3 P1 全表**：实际 34 行（7+9+10+8），编号与分片 P1 全集逐一对报，无重复、无遗漏；`SM-P1-05` 正确缺席。
5. **链接**：§1 表 10 条相对链接 + 正文引用的第 11 份核销专片，目标文件全部存在。

---

## 5. 运行验证与核销审计

- §5 的 14 行运行验证：13 行「确认」，逐项数字与 `/tmp/runtime-evidence-20260907.md` 一致（129 测试、02:07、750/39 源文件、2.42s fail-fast、2.155s 启动、100 表 = 78 + 22、HTTP 与业务码、三组 WS 探针结果）。唯一「部分成立」是具名 InnoDB 表：结论正确（活库 `information_schema` 实测 + `project006.sql` 印证），但当时证据文件只转录了 22 张 MyISAM 清单。**已回填**：证据文件追加 13 张具名表的 `engine` 实测行。
- §5 引用的源码位置全部核实：`WebSocketUnionService.java:78`（`findUserEntityById`）与 `:81`（`getUserName` 无判空）、`application.yml:84-88`（迁移硬约束注释 + `generation: validate`）、`TestController.java:33-41` 四次 DAO 写 + `:42-44` 三行 `System.out.println`。
- §6 的跨轮核销数字与 `2026-09-07-remediation-verification.md` 的三张核销表一致（8/8 P0、fix-spec 23/24、改级 15 行 6/1/2/6、遗留 13 条 10/3）。审计另抽查上一轮 P0 #1/#2/#3/#4/#8/#22 的修复证据，在 `TestPaperService:62-88`、`TheoryKnowledgeService:243-256`、`MenusService:98-126`、`TheoryKnowledgeExamService:75-79`、`WebSocketUnionService:54/60`、`PostMilitaryTermTrainService:136/150/178` 全部真实存在。
- §8 的 4 条「已消除」项全部成立。附带发现：`theory-user` 分片 §5/§6 使用的表名 `t_test_paper_question` 在活库与快照中都不存在，真实表名是 `t_theory_knowledge_test_paper_question`（实体 `@Entity(name=...)` + `CustomPhysicalNamingStrategy` + `project006.sql:29559` 三方印证，且为 InnoDB）；汇总 §8 已使用正确表名，结论不受影响。

---

## 6. 分片文档的残余瑕疵（不影响汇总结论，供后续订正）

| 分片 | 瑕疵 | 正确值 |
|---|---|---|
| `2026-09-07-common-infra-review.md` | 架构说明称 `common/exception` 下有 **7** 个 `@Provider` ExceptionMapper | 实为 **6** 个（Global/IllegalState/InvalidTitle/Unauthorized/Validation/WebApplication）；汇总写 6，正确 |
| `2026-09-07-theory-user-service-review.md` | 使用不存在的表名 `t_test_paper_question` | `t_theory_knowledge_test_paper_question`（InnoDB）|
| `2026-09-07-persistence-review.md` | 页首第 1 行「P3 3-项归并后为 2」措辞含混 | 其权威计数行与实际行数均为 P3 2 |
| `2026-09-07-controller-api-review.md` | 原将 `GET /api/test/start` 定级 P0，理由含「MyISAM 不可回滚」| 已在该分片内标注主代理运行复核结论并改级 P1 |

---

## 7. 审计边界

- 全程只读：未修改源码、未运行构建/测试；`§5` 的运行数字采信主代理证据文件与源码位置核对，审计代理未重跑服务。
- 未复核项：上一轮 2026-08-26 文档内部的「338 条」等历史数字（跨文档、非本轮结论）；`§3` 之外的 P2/P3 条目（139 + 53 条）未逐条复核，仅通过计数对账确认其行数与编号完整性——如需 P2/P3 级的逐条复核，需再开一轮审计。
- 结论有效范围：HEAD `b9b9f22`、工作树干净、活库为 docker `mysql-project006`（未执行迁移 01/02，22 张 MyISAM 仍在）。
